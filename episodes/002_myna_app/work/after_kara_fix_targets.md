# `後から` 発音修正対象

確認日: 2026-08-30

本編80segmentのナレーション本文を検索した結果、対象は次の1segmentのみ。

| segment | 元文 | `後から` の位置（1-based） | 修正 |
|---:|---|---:|---|
| 45 | スマホには「後からインストールしたアプリが優先して開く」という仕組みがあり、デジタル認証アプリを後から入れ直すと、マイナアプリの代わりに開いてしまうことがあるそうです。 | 7, 49 | `後から` → `あとから` |

`後` 単体は文脈依存のため、グローバル辞書登録しない。
