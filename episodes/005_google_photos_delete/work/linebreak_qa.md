# Episode 005 line-break QA

確認日: **2026-09-01**  
対象: `episode.json` の字幕65件とscene overlay 25件

## 共通ルール

Episode 004で恒久化した `wrap_text()` のルールを適用した。

- 日本語として自然な意味単位で改行する
- 行頭禁則文字（、。，．・：；！？％）」』】〕〉｝＞ー…）を行頭に置かない
- 行末禁則文字（（「『【〔〈｛＜）を行末に残さない
- 1文字だけの孤立行、助詞だけの行、固有名詞・機能名の途中分断を作らない
- 明示改行（`\n`）は意味単位として尊重する
- 字幕の安全幅は1800px（72px基準）を維持。今回、padding変更・font size縮小は不要だった

## 字幕の修正記録

長文はナレーション本文を変更せず、表示字幕を意味のまとまりが読める簡潔な文に整理した。

| subtitle | before | after | reason |
|---|---|---|---|
| SUB-004 | `結論から言うと、Googleフォトの「削除」と` / `「空き容量を増やす」は、よく似ていますが、意味が違います。` | `結論から言うと、Googleフォトの「削除」と` / `「空き容量を増やす」は、意味が違います。` | 「よく似ていますが」を省略し、2つの操作名と結論を自然な意味単位で分離。機能名を途中分断しない |
| SUB-015 | `オンになっていて、「バックアップが完了しました」と` / `表示されていれば、写真のコピーは、Google側にあります。` | `「バックアップが完了しました」と` / `表示されていれば、コピーはGoogle側にあります。` | UI確認語を一まとまりにし、重複する「オン」「写真の」を省略。公式UI文言を途中分断しない |
| SUB-020 | `Googleフォトで「削除」すると、` / `スマホ本体にあった、同じ写真も、いっしょに消えます。` | `Googleフォトで「削除」すると、` / `スマホ本体の写真も、いっしょに消えます。` | 「にあった」「同じ」を省略して、削除対象の意味を保った自然な2行に整理 |
| SUB-023 | `バックアップ済みの写真は、ゴミ箱に60日間、残ります。` | `バックアップ済みの写真は、` / `ゴミ箱に60日間、残ります。` | 保持対象と保持期間を意味単位で分離 |
| SUB-032 | `メニューの場所は、機種やアプリのバージョンによって、` / `違う場合があります。` | `メニューの場所は、機種やアプリのバージョンで、` / `違う場合があります。` | 「バージョン」を途中で分断せず、文意を保った自然な接続へ整理 |
| SUB-034 | `次は、「このデバイスの空き容量を増やす」という機能です。` | `「このデバイスの空き容量を増やす」機能です。` | 長い機能名を1行で保持し、前置き語を省略 |
| SUB-044 | `「空き容量を増やす」で空くのは、` / `スマホ本体の容量だけで、Google側の容量は、変わりません。` | `「空き容量を増やす」は、スマホ本体の容量だけです。` / `Google側の容量は、変わりません。` | 本体容量とGoogle側容量の対比を2行に分離。機能名・「Google側」を途中分断しない |
| SUB-045 | `Google側の容量を空けたいときは、` / `写真を削除して、ゴミ箱からも、完全に削除する必要があります。` | `Google側の容量を空けたいときは、写真を削除して、` / `ゴミ箱からも、完全に削除する必要があります。` | 「写真を削除して」を前半の操作単位へ移し、ゴミ箱からの完全削除を後半へ分離 |
| SUB-047 | `iPhone本体にある写真と、Googleフォトにバックアップされた写真では、` / `削除したときの扱いが違います。` | `iPhone本体の写真と、` / `Googleフォトのバックアップ写真は別の扱いです。` | iPhone・Googleフォト・バックアップ写真を分断せず、比較の主旨を短い2行で明示 |
| SUB-051 | `「最近削除した項目」は、` / `iPhoneのゴミ箱のような場所で、30日間、保管されます。` | `「最近削除した項目」は、` / `iPhoneのゴミ箱のような場所。30日間保管されます。` | 「最近削除した項目」を1行目に固定し、場所と30日を2つの意味単位に整理 |
| SUB-062 | `「削除」は、スマホとGoogleフォトの、両方から消えます。` | `「削除」は、スマホとGoogleフォトの、` / `両方から消えます。` | 操作名と対象を1行目にまとめ、短すぎる1行を避けて「両方から」を結果として分離 |
| SUB-064 | `間違えて消しても、ゴミ箱に残っている間は、復元できます。` | `間違えて消しても、` / `ゴミ箱に残っている間は、復元できます。` | 条件と結果を意味単位で分離 |

## Scene overlayの修正記録

| scene | 対象 | before | after | reason |
|---|---|---|---|---|
| SCENE-020 | support_text | `iPhoneでは、Googleフォトのゴミ箱と、「最近削除した項目」の、両方を確認しましょう。` | `iPhoneでは、Googleフォトのゴミ箱と、` / `「最近削除した項目」の、両方を確認しましょう。` | 公式アルバム名を途中分断せず、2つのゴミ箱の比較単位で明示改行 |
| SCENE-025 | headline | `あわてずに、確認しましょう` | `あわてずに、` / `確認しましょう` | 「確認」を途中分断せず、読点位置で自然に分割 |

## 修正後の機械検査

- subtitle width overflow: **0件**
- subtitle linebreak warning: **0件**
- scene `linebreak_issues()`: **0件**
- 1文字孤立行: **0件**
- 助詞だけの行: **0件**
- 保護語（Googleフォト / バックアップ / デバイスから削除 / 最近削除した項目）の途中分断: **0件**

## draft_v3追補（2026-09-02、人間レビュー反映）

| scene | 対象 | before | after | reason |
|---|---|---|---|---|
| SCENE-020 | compare card | items文字列1本（タイトルに60日（バッ…が切れる） | title/body構造へ分割し、bodyを「60日（バックアップ済み）／\\n30日（未バックアップ）」に | 括弧内容・数字＋単位を分断しない自然な2行 |
| SCENE-007 | support_text | 「…バックアップをオンにしている、ほ／かの端末からも…」 | 「スマホ本体も、バックアップがオンの\\n他の端末も、いっしょに消えます。」 | 「ほ／か」の仮名分断を解消、2行の意味単位へ整理 |
| SCENE-013 | headline | 「このデバイスの空き容量を増やす」が途中分断 | 「このデバイスの\\n空き容量を増やす」 | 機能名「空き容量を増やす」を1行で保持 |
| SCENE-019 | support_text | 「…場所で、30／日間保管…」「完全に削除され／ます。」 | 「ゴミ箱のような場所。30日間保管され、\\n30日を過ぎると完全に削除されます。」 | 数字＋単位・動詞活用の分断を解消 |

## 恒久化（draft_v3）

- `scene_renderer.py` wrap: 開き括弧（で改行を送り、括弧内を1単位で保持（60日（バッ／クアップ済み）禁止）。
- `phase2_qa.py`: 括弧内改行・閉じ括弧行頭（）」』）・行末開き括弧（（「『）・保護語またぎ・数字と単位の分断（30／日）・compareカードtitle/body対象化を追加。
- `subtitle_preflight.py`: PROTECTED_TERMSへ「バックアップ／バックアップ済み／未バックアップ／空き容量を増やす／デバイスから削除／最近削除した項目」を追加。

## 最終結果（draft_v3時点）

- linebreak issues: **0件** / subtitle width overflow: **0件** / subtitle FAIL・WARN: **0件**
